{
  "name": "neynar-test",
  "version": "0.0.5",
  "description": "Test sdk for neynar",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del-cli ./build/*",
    "build": "yarn run clean && tsc"
  },
  "author": "neynar",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.4",
    "del-cli": "^5.1.0",
    "typescript": "^5.2.2"
  }
}
