{
  "name": "neynar-test",
  "version": "0.0.2",
  "description": "Test sdk for neynar",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del-cli ./build/*",
    "build": "yarn run clean && tsc"
  },
  "author": "neynar",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.1.0",
    "typescript": "^5.2.2"
  }
}
